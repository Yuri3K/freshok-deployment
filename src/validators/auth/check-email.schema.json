{
  "$id": "validators/auth/check-email.schema.json",
  "title": "Check Email Existence",
  "additionalProperties": false,
  "required": ["email"],
  "properties": {
    "email": {
      "type": "string",
      "format": "email",
      "maxLength": 255,
      "description": "Email address to check for existence in the users collection"
    }
  },

  "errorMessage": {
    "required": {
      "email": "Email field is required"
    },
    "properties": {
      "email": "Incorerct email address"
    },
    "additionalProperties": "The request body contains invalid fields."
  }
}

